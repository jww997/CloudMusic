import P from '@/apis/listen/typeParams';
import R from '@/apis/listen/typeResult';
import T from '@/types'
export type TAG = T.TAG

export interface PARAMS extends P.PARAMS_ARTIST_LIST { }

export interface RESULT extends R.RESULT_ARTIST_LIST { }